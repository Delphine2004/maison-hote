<form
    method="post"
    action="{{ path('app_room_delete', { id: room.id }) }}"
    onsubmit="return confirm('Confirmez vous la suppression?');"
>
    <input
        type="hidden"
        name="_token"
        value="{{ csrf_token('delete' ~ room.id) }}"
    />
    <button class="btn w-100">Supprimer</button>
</form>
