{% extends 'base.html.twig' %} {% block title %}Création réservation{% endblock
%} {% block body %}
<main class="pt-5">
    <section class="container-fluid py-5">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-md-10 col-lg-8">
                    <h1 class="text-center m-5">Ajouter une réservation</h1>
                    <div
                        class="card bg-blue border-gold shadow-sm d-flex justify-content-evenly p-4"
                    >
                        {{ include("booking/_form.html.twig") }}
                    </div>
                </div>
            </div>
            <div class="text-center m-5">
                <a class="btn" href="{{ path('app_booking_index') }}"
                    >Retour aux réservations</a
                >
            </div>
        </div>
    </section>
</main>
{% endblock %}
