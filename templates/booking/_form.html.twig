{{ form_start(form, {'attr': {'novalidate':  true}}) }}
<div class="mb-4 form-check text-gold">
	{{ form_widget(form.bookingConsent, {'attr': {'class': 'form-check-input'}}) }}
	<label class="form-check-label">
		J'accepte le traitement de mes données conformément à la
		<a href="{{ path('app_privaty_policy') }}">politique de confidentialité</a>.
	</label>
	{{ form_errors(form.bookingConsent) }}
</div>
<div class="text-center m-2">
	<button class="btn">{{ button_label|default('Réserver') }}</button>
</div>
{{ form_end(form) }}
