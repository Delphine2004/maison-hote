<form
    method="post"
    action="{{ path('app_booking_cancel_staff', { id: booking.id }) }}"
    onsubmit="return confirm('Confirmez vous l\'annulation?');"
>
    <input
        type="hidden"
        name="_token"
        value="{{ csrf_token('cancelbystaff' ~ booking.id) }}"
    />
    <button class="btn w-100">Annuler</button>
</form>
