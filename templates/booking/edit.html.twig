{% extends 'base.html.twig' %} {% block title %}Edit Booking{% endblock %} {%
block body %}

<section class="container-fluid">
    <div class="container">
        <h1>Edit Booking</h1>
        <div class="mx-auto">
            {{
                include("booking/_form.html.twig", { button_label: "Modifier" })
            }}
        </div>
        <div class="m-5">{% include 'partials/_flash.html.twig' %}</div>

        <div class="text-center m-5">
            <a
                class="btn"
                href="{{
                    path('app_event_show', {
                        id: booking.id
                    })
                }}"
                >Revenir à la réservation</a
            >
            <a class="btn" href="{{ path('app_booking_index') }}"
                >back to list</a
            >
            {{ include("booking/_delete_form.html.twig") }}
        </div>
    </div>
</section>

{% endblock %}
