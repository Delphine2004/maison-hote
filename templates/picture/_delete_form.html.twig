<form method="post" action="{{ path('app_picture_delete', {'id': picture.id}) }}" onsubmit="return confirm('Confirmez vous la suppression?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
	<button class="btn w-100">Supprimer</button>
</form>
