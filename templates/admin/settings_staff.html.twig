{% extends 'base.html.twig' %}
{% block title %}Les Parenthèses Dorées - Paramétrages
{% endblock %}
{% block body %}
	<main class="py-5">

		<section class="container-fluid">
			<div class="container">

				<div class="text-center mt-5">
					<a class="btn" href="{{ path('app_settings_index') }}">Retour aux paramétres</a>
				</div>

				<div class="row justify-content-center">
					<div class="col-12 col-md-10 col-lg-8">

						<div class="card border-gold shadow-sm my-5">
							<div class="card-header bg-blue text-gold py-3">
								<h1 class="text-center m-3">Utilisateurs</h1>
							</div>

							<div class="card-body p-0">
								{% for user in users %}
									<div class="card shadow-sm d-flex flex-md-row m-3">
										<div class="card-body d-flex flex-row justify-content-between align-items-center p-3">
											<p class="m-0">{{ user.email }}</p>
											<p class="m-0">
												{% for role in user.roles %}
													{% if role != 'ROLE_USER' %}
														{{ role|replace({'ROLE_': ''}) }}
													{% endif %}
												{% endfor %}
											</p>
										</div>
										<div class="d-flex flex-row justify-content-center align-items-center p-3">
											<a class="btn w-100" href="{{ path('app_user_show', { id: user.id }) }}">Voir</a>
										</div>
									</div>
								{% else %}
									<p class="text-center m-2">
										Aucun utilisateur trouvé.
									</p>
								{% endfor %}
							</div>
							<div class="card-footer bg-blue py-5">
								<div class="row justify-content-evenly g-3">
									<div class="col-12 col-md-4">
										<a class="btn w-100" href="{{ path('app_staff_new') }}">Ajouter un utilisateur</a>
									</div>
								</div>
							</div>
						</div>

					</div>
				</div>

			</div>
		</section>
	</main>
{% endblock %}
