{% extends 'base.html.twig' %} {% block title %}Paramétrage - Création
utilisateur{% endblock %} {% block body %}
<main class="py-5">
    <section class="container-fluid">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-md-10 col-lg-8">
                    <div
                        class="card bg-blue border-gold shadow-sm d-flex justify-content-evenly p-4"
                    >
                    <h1 class="text-center text-gold m-5">Ajouter un utilisateur</h1>
                        {{ include("user/_form.html.twig") }}
                    </div>
                </div>
            </div>
            <div class="text-center m-5">
                <a class="btn" href="{{ path('app_staff_index') }}"
                    >Retour aux utilisateurs</a
                >
            </div>
        </div>
    </section>
</main>
{% endblock %}
