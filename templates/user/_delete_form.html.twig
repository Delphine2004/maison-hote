<form
    method="post"
    action="{{ path('app_client_delete', { id: user.id }) }}"
    onsubmit="return confirm('Confirmez vous la suppression?');"
>
    <input
        type="hidden"
        name="_token"
        value="{{ csrf_token('delete' ~ user.id) }}"
    />
    <button class="btn w-100">Supprimer</button>
</form>
